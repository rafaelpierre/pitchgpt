[tox]
envlist = test_service
skipsdist = true

[testenv]
deps = -r test-requirements.txt
allowlist_externals = rm
install_command = 
  pip install -U pip
  pip install -e ./pitchgpt
  pip install {opts} {packages}

[testenv:lint]
basepython = python 
commands = ruff pitchgpt

[testenv:fix]
basepython = python 
commands = ruff pitchgpt --fix

[testenv:test_service]
deps = -r test-requirements.txt

setenv =
  PYTHONPATH=.

commands =
    pytest \
      {tty:--color=yes} \
      pitchgpt/tests/{posargs} \
      --log-cli-level=DEBUG \
      --log-format='%(asctime)s %(levelname)s %(module)s:%(lineno)d %(message)s' \
      --cov=pitchgpt \
      --cov-report xml